spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: zyj020402
    url: jdbc:mysql://hadoop103:3306/gulimall_wms

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

server:
  port: 11000